#include <stdio.h>

// Declare multple variables of the same type separated by commas
int main() {
  int x = 5, y = 10, z = 15;
  // Print sum of vars with one format specifier
  printf("%d\n", x + y + z);
}
