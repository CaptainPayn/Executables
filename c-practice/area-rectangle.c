#include <stdio.h>

int main() {
  int length = 4;
  int width = 6;
  int area; // Declare area
  // Calculate the area
  area = length * width;

  // Print results
  printf("A rectangle with length %d and width %d\n", length, width);
  printf("Has an area of %d\n", area);
  return 0;
}
